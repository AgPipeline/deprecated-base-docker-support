language: python

python:
  - "3.7"

cache: pip

install: 
 - "pip install pytest"
 - "pip install pylint"

script:
 - "pylint --rcfile=.pylintrc base-image/*.py"
 - "pylint --rcfile=.pylintrc base-image/test-files/*.py"
 - "cd base-image"
 - "python -m pytest -v"